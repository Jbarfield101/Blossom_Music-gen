{
  "type": "object",
  "required": ["id", "instrument", "meter", "density", "length_bars", "midi_file"],
  "properties": {
    "id": { "type": "string" },
    "instrument": { "type": "string", "enum": ["drums", "bass", "keys", "pads"] },
    "meter": { "type": "string" },
    "density": { "type": "string", "enum": ["sparse", "med", "busy"] },
    "length_bars": { "type": "integer", "minimum": 1 },
    "midi_file": { "type": "string" },
    "style": { "type": "string" },
    "tags": { "type": "array", "items": { "type": "string" } }
  }
}
